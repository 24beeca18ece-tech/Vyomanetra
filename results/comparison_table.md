# VyomAnetra vs. ExoVeil -- baseline comparison

BLS/trapezoid pipeline (this work) vs. ExoVeil (Priyanshu 2026, arXiv:2606.02778, PyPI `exoveil==0.2.1`), run on the identical Savitzky-Golay-flattened flux array for each target (`download_lc()` + `detrend()` shared between both pipelines).

**Note on ExoVeil's output**: the shipped v0.2.1 package performs raw, non-phase-folded, single-transit-event detection only -- `detect_from_array()` returns a list of candidate events (`time`, `snr`, `depth_ppm`, `duration_pts`, `uncertainty_category`), each independently scored. It does **not** expose an orbital period/epoch, and does **not** expose the planet-vs-false-positive XGBoost classification or conformal-interval output described in the paper text -- those either aren't wired into the public API at this version, or require a separate call not documented in the package.

**Phase-fold methodology**: `summary_table.csv` only stores BLS's period, not its epoch (t0), so t0 was recomputed by re-running prototype.py's own `identify()` (`compute_bls_ephemeris.py`, prototype.py itself untouched) -- recovered periods matched `summary_table.csv` exactly. Each ExoVeil event time is folded against (BLS period, BLS t0); "in-transit" means the folded offset falls within ±(fitted total transit duration)/2 from `summary_table.csv`. "ExoVeil top event" is the highest-SNR event returned (default `threshold=5.0`).

**Note on the V1828 Aql SNR value**: the top event's raw SNR (~1.09e7) is flagged `SNR undefined (near-zero local noise)` rather than clipped or dropped -- it is kept in `exoveil_top_snr_raw` for transparency, but should not be read as a detection significance. See per-target notes for the mechanism.

**Pi Mensae only**: additionally cross-checked against the refined Pi Men c ephemeris of Larsen et al. (2026, arXiv:2607.12088, Table 2: P=6.267823±1e-06 d, T0=1325.5042±0.0003 BTJD, T14=2.969 h from their Table 1) rather than the BLS period for this target, which is itself flagged 'POSSIBLE STELLAR VARIABILITY/ALIAS' and is not Pi Men c's real period. The refined ephemeris's period uncertainty is ~1 order of magnitude smaller than the prior (Gandolfi/Huang 2018-era) solution, per that paper's abstract. Propagated to our data's epoch this uncertainty is <1% of the transit half-duration window (see per-target note) -- small enough that the resulting in-transit count is a genuine non-detection check, not an ephemeris-precision artifact. See the "Events in-transit (lit. ephem.)" column and per-target notes for the full propagation.

| Target | True label | BLS P (d) | BLS t0 (BTJD) | BLS depth (ppm) | BLS SNR | BLS flag | ExoVeil #events | ExoVeil top SNR (raw) | SNR flag | ExoVeil top depth (ppm) | ExoVeil top t (BTJD) | Uncertainty | Phase offset (h) | In-transit? | Events in-transit (BLS ephem.) | Events in-transit (lit. ephem.) | Notes |
|---|---|---|---|---|---|---|---|---|---|---|---|---|---|---|---|---|
| TOI-700 | Planet | 16.0510 | 1340.085495 | 2247.2 | 13.9 | PLANET CANDIDATE | 12 | 6.0645 | - | 397.0 | 1401.890 | data_limited | -57.59 | NO | 0/12 | n/a | Top ExoVeil event folds to -57.59 h from nearest predicted transit center (needs within ±0.63 h) -> OUT-OF-TRANSIT (does not match BLS ephemeris). 0 of the returned ExoVeil events fall in-transit against the BLS ephemeris. 12 candidate events in total; phase-fold uses the BLS 16.05 d period/t0 (itself possibly an alias of the system's actual ~10-37 d multi-planet ephemerides, which BLS is not guaranteed to recover from a single dominant peak). |
| TOI-1338 | Planet | 14.6083 | 1424.471848 | 59619.8 | 742.9 | LIKELY ECLIPSING BINARY | 16 | 10.2646 | - | 4814.2 | 1453.788 | data_limited | +2.39 | YES | 7/16 | n/a | Top ExoVeil event folds to +2.39 h from nearest predicted transit center (needs within ±2.67 h) -> IN-TRANSIT (phase-agrees with BLS). 7/16 returned ExoVeil events fall in-transit against the BLS ephemeris. Circumbinary system: BLS flags 'LIKELY ECLIPSING BINARY' at 59,620 ppm (almost certainly the EB eclipse, not the circumbinary planet transit) -- so this phase-fold check verifies agreement with the EB signal, not with TOI-1338 b. Neither pipeline isolates the actual planet signal in this run. |
| Pi Mensae | Planet | 27.2149 | 1339.602960 | 3493.6 | 151.1 | POSSIBLE STELLAR VARIABILITY/ALIAS | 20 | 15.6199 | - | 1059.0 | 1599.171 | data_limited | -301.94 | NO | 0/20 | 0/20 | Top ExoVeil event folds to -301.94 h from nearest predicted transit center (needs within ±1.36 h) -> OUT-OF-TRANSIT (does not match BLS ephemeris). 0 of the returned ExoVeil events fall in-transit against the BLS ephemeris. Our Pi Mensae light curve draws on TESS sectors 1, 4, 8, 11 (confirmed via the lightkurve search results, same selection order download_lc() uses), spanning BTJD 1330.0-1613.6. Checked against BLS's own 27.21 d period (not Pi Men c's real period): event 1 (SNR 15.62, depth 1059 ppm) folds to -301.94 h -> out-of-transit; event 2 (SNR 15.56, depth 842 ppm) folds to -247.83 h -> out-of-transit. A mismatch here reflects BLS locking onto a different (likely aliased) period rather than necessarily meaning ExoVeil is wrong. Cross-checked instead against the refined Pi Men c ephemeris of Larsen et al. (2026, arXiv:2607.12088, "TESS Photometry and Radial Velocity Analysis of the sub-Neptune Exoplanet Pi Mensae c...", Table 2; 20 TESS sectors S1-S95): P=6.267823±1e-06 d, T0=1325.5042±0.0003 BTJD, T14=2.969 h -- top-2: event 1 folds to -50.81 h -> out-of-transit; event 2 folds to -55.97 h -> out-of-transit. Extending to **all 20 returned events**: 0/20 land in-transit. Ephemeris-uncertainty check: propagating T0/P uncertainty (N_cycles x sigma_P, quadrature with sigma_T0) out to our most-distant event (46.0 cycles from T0) gives a timing uncertainty of only 0.44 min (0.49% of the ±1.48 h half-duration window) using the refined ephemeris -- vs. 1.13 min with the prior (pre-2607.12088, ~10x larger period-error) solution. Even the OLD ephemeris was already precise enough here; the refined one makes the margin larger still. Since the propagated uncertainty is <1% of the search window, the near-total absence of in-transit events (0-1/20 depending on ephemeris used, vs. top-2 SNR ~15.6/15.6) is NOT an ephemeris-precision artifact -- it stands as a genuine non-detection of a known, previously-confirmed planet by ExoVeil at threshold=5.0 in this run, not just a top-event mismatch. |
| V1828 Aql | Eclipsing Binary | 0.6623 | 2770.393644 | 25480.5 | 24.3 | LIKELY ECLIPSING BINARY | 20 | 1.08571e+07 | SNR undefined (near-zero local noise) | 3309.3 | 3530.583 | confident | -2.21 | NO | 1/20 | n/a | Top ExoVeil event folds to -2.21 h from nearest predicted transit center (needs within ±0.16 h) -> OUT-OF-TRANSIT (does not match BLS ephemeris). 1/20 returned ExoVeil events fall in-transit against the BLS ephemeris. EB (P=0.66d) outside ExoVeil's validated regime. Top event's raw SNR (1.086e+07) is a numerical artifact, not a real detection significance: exoveil.detect.detect_events() weights residuals by 1/local_MAD(residual); the EB's sharp, near-total eclipse drives the local MAD toward ~0 in that window, blowing up the ratio. Second-ranked event (SNR=62.0, t=3528.67 BTJD) is a more representative 'top' detection for this target -- included in all_events for reference. That 2nd-ranked event folds to -0.53 h from transit center -> out-of-transit. Illustrates the single-transit matched filter breaking numerically on non-planet, high-contrast periodic signals. |
